# Core runtime
discord.py>=2.4.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Active AI providers (primary + fallback — both required)
groq>=0.11.0
openai>=1.50.0

# ── Backup providers — uncomment if you swap ACTIVE_AI_PROVIDER ───────────────
# anthropic>=0.39.0          # ACTIVE_AI_PROVIDER=claude
# google-generativeai>=0.8.0 # ACTIVE_AI_PROVIDER=gemini
# cerebras-cloud-sdk>=1.0.0  # ACTIVE_AI_PROVIDER=cerebras
