# Primary LLM key — used by both the browser agent (langchain_google_genai)
# and the summariser (google-generativeai), matching the llm_backup setup.
GEMINI_API_KEY=your_gemini_key_here

# --- Future / optional keys (add as needed for additional free-tier quotas) ---
# GEMINI_API_KEY_2=your_second_gemini_key_here
# OPENROUTER_API_KEY=your_openrouter_key_here
